import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* * TEMPORARY: Headers removed to unblock development.
   * We will add security back once the app is running.
   */
};

export default nextConfig;